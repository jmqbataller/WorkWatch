(() => {
  const OWNER='system_admin',pro=window.WorkWatchPro||{},suite=window.WorkWatchSuite||{},baseRenderPage=renderPage;
  renderPage=function(role){const r=baseRenderPage(role);if(role===OWNER&&state.view==='settings')queueMicrotask(patchSettings);return r;};
  function patchSettings(){if(state.profile?.role!==OWNER||state.view!=='settings'||document.getElementById('proAdvancedSettings'))return;const p=suite.preferences||{},section=document.createElement('section');section.className='panel pro-advanced-settings';section.id='proAdvancedSettings';section.innerHTML=`<div class="panel-head"><div><div class="panel-title">Evidence, focus & privacy</div><div class="panel-desc">Advanced personal settings for evidence watermarking, idle checks, and focus sessions.</div></div></div><div class="panel-body pro-form">${pro.schemaReady?'':'<div class="pro-schema-warning"><strong>Database upgrade pending.</strong> New advanced features will activate after the latest Supabase migration is applied.</div>'}<label class="suite-check"><input type="checkbox" id="proWatermark" ${p.watermark_enabled!==false?'checked':''}> Add automatic WorkWatch watermark to new screenshots</label><div class="field"><label>Watermark label</label><input class="input" id="proWatermarkText" maxlength="80" value="${esc(p.watermark_text||'WorkWatch')}"></div><div class="pro-two"><div class="field"><label>Idle confirmation after</label><select class="select" id="proIdle"><option value="5" ${Number(p.idle_prompt_minutes)===5?'selected':''}>5 minutes</option><option value="10" ${Number(p.idle_prompt_minutes)===10?'selected':''}>10 minutes</option><option value="15" ${Number(p.idle_prompt_minutes||15)===15?'selected':''}>15 minutes</option><option value="30" ${Number(p.idle_prompt_minutes)===30?'selected':''}>30 minutes</option><option value="60" ${Number(p.idle_prompt_minutes)===60?'selected':''}>60 minutes</option></select></div><div class="field"><label>Default focus / break</label><div class="pro-two"><input class="input" id="proFocusWork" type="number" min="5" max="180" value="${Number(p.focus_work_minutes||50)}"><input class="input" id="proFocusBreak" type="number" min="1" max="60" value="${Number(p.focus_break_minutes||10)}"></div></div></div><div class="task-actions"><button class="btn" id="proNotify" type="button">Enable notifications</button><button class="btn btn-primary" id="saveProSettings" type="button">Save advanced settings</button></div></div>`;page()?.appendChild(section);const shortcuts=document.createElement('section');shortcuts.className='panel pro-advanced-settings';shortcuts.innerHTML=`<div class="panel-head"><div><div class="panel-title">Keyboard shortcuts</div><div class="panel-desc">Fast controls for your daily workflow.</div></div></div><div class="panel-body pro-shortcuts"><div><span>Command search</span><kbd>Ctrl K</kbd></div><div><span>Start task</span><kbd>Alt S</kbd></div><div><span>Pause / Resume</span><kbd>Alt P</kbd></div><div><span>Quick evidence</span><kbd>Alt E</kbd></div><div><span>Finish task</span><kbd>Alt F</kbd></div><div><span>Search</span><kbd>/</kbd></div></div>`;page()?.appendChild(shortcuts);const delivery=document.createElement('section');delivery.className='panel pro-advanced-settings';delivery.innerHTML=`<div class="panel-head"><div><div class="panel-title">Client email delivery</div><div class="panel-desc">Finalized reports can be sent from WorkWatch through Brevo.</div></div></div><div class="panel-body"><p>Server delivery uses <code>BREVO_API_KEY</code>, <code>BREVO_SENDER_EMAIL</code>, and optionally <code>BREVO_SENDER_NAME</code> in Supabase Edge Function secrets. If they are not configured, WorkWatch automatically falls back to your default email app with the secure report link prepared.</p></div>`;page()?.appendChild(delivery);document.getElementById('saveProSettings').onclick=save;document.getElementById('proNotify').onclick=async()=>{if(!('Notification'in window))return toast('Browser notifications are not supported.','error');const x=await Notification.requestPermission();toast(x==='granted'?'Notifications enabled.':'Notifications were not enabled.',x==='granted'?'':'error');};}
  async function save(){const payload={user_id:state.profile.id,watermark_enabled:document.getElementById('proWatermark').checked,watermark_text:document.getElementById('proWatermarkText').value.trim()||'WorkWatch',idle_prompt_minutes:Number(document.getElementById('proIdle').value),focus_work_minutes:Number(document.getElementById('proFocusWork').value),focus_break_minutes:Number(document.getElementById('proFocusBreak').value),updated_at:new Date().toISOString()};const {error}=await sb.from('user_preferences').upsert(payload,{onConflict:'user_id'});if(error)return toast(error.message,'error');Object.assign(suite.preferences||{},payload);await pro.refresh?.();toast('Advanced settings saved.');}
  const observer=new MutationObserver(()=>{if(state.profile?.role===OWNER&&state.view==='settings')patchSettings();});observer.observe(document.documentElement,{childList:true,subtree:true});
})();
